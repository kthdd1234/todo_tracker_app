import 'package:flutter/material.dart';

class CommonNull extends StatelessWidget {
  const CommonNull({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
